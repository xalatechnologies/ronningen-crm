/**
 * Toasts are implemented with Sonner (`sonner.tsx`), which is the current
 * shadcn/ui recommendation. Import `Toaster` from `@/components/ui/sonner`.
 */
export { Toaster } from "@/components/ui/sonner";
