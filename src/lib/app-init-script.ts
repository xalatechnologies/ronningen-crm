/** Inline blocking init — runs before paint to avoid theme/density flash. */
export const APP_INIT_INLINE_SCRIPT = `(function(){try{var key=document.documentElement.getAttribute("data-theme-storage-key")||"ronningen-crm";var theme="light";var raw=localStorage.getItem(key);if(raw){var parsed=JSON.parse(raw);var stored=parsed&&parsed.state&&parsed.state.theme;if(stored==="light"||stored==="dark"){theme=stored}else if(stored==="system"){theme="light"}}var resolved=theme;document.documentElement.setAttribute("data-theme",resolved);if(resolved==="dark"){document.documentElement.classList.add("dark")}else{document.documentElement.classList.remove("dark")}}catch(_error){}})();(function(){try{var key=document.documentElement.getAttribute("data-display-storage-key")||"ronningen-crm";var raw=localStorage.getItem(key);if(!raw)return;var parsed=JSON.parse(raw);var d=parsed&&parsed.state&&parsed.state.displayDensity;if(d==="compact"||d==="comfortable"||d==="spacious"){document.documentElement.setAttribute("data-density",d)}}catch(e){}})();`;
